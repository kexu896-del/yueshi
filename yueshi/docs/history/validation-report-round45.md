# round45 验证报告

## 新增测试

tests/test_round45_speed_and_caliber.py：6 组 25 断言，全部通过。

- t01 联合求解器：一次 optimal；双人碳水/蛋白/热量/脂肪全达标；locked 食材不进调整输出；L1 目标与调整记录非空
- t02 infeasible 明确返回；CLI optimal exit 0、infeasible exit 3
- t03 大米门禁：熟重充当生重被拦截、缺熟饭对应量被拦截、"生米约1000g（对应熟米饭约2300g）"通过
- t04 非做饭日旧文案被渲染门禁拦截；标准文案入 output-policy
- t05 取整口径（≤0.9g tolerance / 约区间显示 / 内外口径一致）与包装优先级、米类生熟规则
- t06 局部重装配七步管线、联合求解纪律、先求解后渲染、哈希缓存——规则文本齐

## 全量回归

43 个测试文件全部通过（pass=43 fail=0），无既有测试需要联动修改（纯增量轮）。

## 求解器实测

合成双人日（4 食材 × 2 成员，6 类约束）：一次求解 optimal，L1=391.2g；约束收紧至不可达时正确返回 infeasible。

## 已知边界

- 求解器按"日"求解；跨日食材联动（余量复用）仍由装配层处理。
- 意见§一.4 带鱼类具体平替选择仍属生成期排序行为，本轮以规则形式固化优先级。
